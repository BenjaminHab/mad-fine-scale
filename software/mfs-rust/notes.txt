Build with 
cargo build

Flash with
cargo flash --chip STM32G070CBTx --release

Flash and start rtt terminal
cargo embed

Move to either embedded-hal(?)/stm32g0(PAC) or embassy_stm32 (or similar)!

Build and flash example: 
cd examples
# cargo build --bin blinky --release
# cargo flash --bin blinky --chip STM32G070CBTx --release

cargo run --bin blinky

Configure CMSIS-SVD file in VSCode extension probe-rs to read the registers of the chip


INA aus -> ADC zeigt genau 0 an (eigentlich sollte das der High Impedance state sein, also warum ist da kein rauschen drauf?)
INA an -> ADC rauscht und zeigt Spektrallinien im Histogramm, geht v.a. auf 4080
Andere ADC-Channels auf dem gleichen Chip rauschen ganz normal (Moment, Korrektur: PA0 zeigt ähnliches Verhalten?)
Gleicher Channel auf anderem Chip rauscht normal, selbst der OP-Amp Channel auf einem anderen Chip rauscht normal und gibt sinnvolle Daten aus, verdammt!!!
Mit der Wheatstone-Brücke abgesteckt und später angesteckt tritt überhaupt kein Rauschen auf, der Wert bleibt exakt auf dem gleichen Niveau???
Mit einem Thermocouple tritt wieder die grobe Quantisierung auf, kein Rauschen, keine Veränderung durch Erwärmen erkennbar (Angesteckt via Breadboard)